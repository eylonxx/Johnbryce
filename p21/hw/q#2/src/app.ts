enum Color {
  Black = 'cola zero',
  White = 'Snow',
  Blue = 'Sea',
  Red = 'coca cola',
  Green = 'leaf',
}

const getColorItem = (a: Color) => {
  return a;
};

console.log(getColorItem(Color.Black));
